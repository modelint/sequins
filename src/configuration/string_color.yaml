# string_color.yaml

# Here we map string names to colors with the intention of
# overriding the String Material style setting as well as the Thread Material style setting
# for any Threads emanating from the same String

elevator:  # theme name
  UI: lime green  # string name : color pairs
  TRANS: aqua
  SIO: magenta